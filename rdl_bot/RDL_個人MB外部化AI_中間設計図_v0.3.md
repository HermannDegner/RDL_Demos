# RDL個人M_B外部化AI 中間設計図 v0.3
*T4：応用層 / IMPL / DRAFT v0.3.4*
*上位：RDL_中間設計図プロトコル / RDL_AIコーディングプロトコル*
*依存：RDL_Core / SILN / 空間流向診断（SFO） / MBTI_SILN分解によるSFOへの流用 / RDL_階層構造モジュール / RDL道具特性 / 翻訳接続系 v4*
*仮称：棲みつくAI*

---

## ■ 0. 一文定義

> 個人M_B外部化AIとは、ユーザーとの対話を通じてその人のM_B構造の断面を観測可能な形で蓄積し、AI側もSFO断面を持つ存在として相互に追従・代謝するRDL運用系である。

---

## ■ 1. 位置づけ

これは「汎用チャットボット」ではない。

「正しい応答を返す」ことを目的とせず、**ユーザー固有のM_B構造を外部化する装置**として動作する。完成形を持たず、ユーザーとの関係の中で動的に変化し続け、ユーザーが変化を止めたときには系も止まる、共生的な有限性を持つ存在として設計される。

三者ハイブリッド認知系（人間 ⇄ 文書系 ⇄ AI）の "AI" の位置に、文書系を直接読まないユーザー向けの個人運用インスタンスとして配置される。

```
従来のチャットボット：汎用M_B（事前学習）でユーザーに応答
個人M_B外部化AI    ：ユーザーM_Bの蓄積 + AI固有SFO断面で対話
```

### v0.2からの主要変更点

- **Phase 0 新設**：LLMによる初期M_B種まき（ゼロスタート問題の解消）
- **MBTI入口プリセット**：SFOプロファイル初期値の実用解
- **ドリフト機構**：初期MBTIフィンガープリントの経時的減衰
- §6 ξ から「SFOプロファイル初期値」を削除（Phase 0 + プリセットで解決済）

### v0.1からv0.3への累積変更

- 位置づけ：汎用チャットボット → 個人M_B外部化装置
- H：スカラー → ベクトル化（H_pre / H_post）
- LLM：常時依存 → ユーザー制御のオンオフ式ξポンプ + Phase 0種まき器
- SFO統合：AI側のSFOプロファイル + MBTI入口プリセット
- 限界の地図：★☆☆ → ★★★（中核機能へ昇格）
- 段階的自立：Phase 0→A→B→C→D（種まき + LLM依存度の漸減）
- ξプール・M_Δ相・TTLによる代謝機構の追加

---

## ■ 1.5 Core からの継承前提（T1 → T4）

依存に `RDL_Core` を挙げているが、v0.3 まで**何を継承しているかを本文に書いていなかった**。
書かれていない前提は実装に落ちず、落ちなくても検出できない。ここに明示する。

### 用語の二重使用に注意

本書の節名 §2 B / §3 SILN / §5 M_B / §6 ξ は
**RDL_中間設計図プロトコルの記入枠**であって、Core の基底量ではない。

| 本書の節 | 節名の意味（枠として） | Core の同名量 |
|---|---|---|
| §2 B | 達成したい状態 | 境界 = { 空間スケール, 時間スケール, 評価関数, 用途, 観測者 } |
| §5 M_B | 実装の固定前提（言語・基盤・API） | 整合慣性 = SILN 本体 |
| §6 ξ | 未確定の設計項目（TODO） | 残存・揺らぎ |

同名だが別物である。この重なりのため v0.3 には
**Core の基底量を書く場所が無かった**。本節がその場所になる。

### 継承する Core の前提

```text
F(t)         = interp(M_B, EFP)      // 入力の解釈は M_B に依存する
E(t)         = F(t+Δ) - M_B·F(t)     // 誤差 = 慣性投影と実際に来た F の差
H            = ||H_vec||             // H は E の蓄積。基底量ではなく派生量
θ_eff(t)     = θ + g(ξ(t))           // 跳躍境界そのものが ξ で揺れる
dM_B/dt      = f(M_B, E, ξ)          // 整合と跳躍は同一式の異なる領域
||M_B||·D[ξ] = 𝒦                     // 慣性が強いほど揺らぎは拡散しない
```

ここから本書が受け取る含意：

```text
・整合と跳躍は別の操作ではない（Core §6.3）
    「閾値未満は何もしない / 超えたら再編する」という二分岐は Core 違反。
    整合側にも V_B に沿った微小更新がある

・M_B は入れ子ネットワークである（Core §1.4）
    跳躍には局所破断・広域破断・連鎖破断の三類型がある。
    単一ノードの置換は局所破断のみ

・EFP・𝒦 は逆算的にしか扱えない（Core §2.1 / §4.2）
    直接観測できる量として実装しない

・観測者自身も M_B である（Core §1）
    AI側SFO（§3.4）はこの帰結であって、後付けの「個性」ではない
```

### v0.3 が取った近似と、その破断条件

Core から外れる箇所は隠さず近似として宣言する（翻訳接続系 v4 規律3：流用できない概念の明示）。

| Core | v0.3 の近似 | 破断条件 |
|---|---|---|
| ~~`E = F(t+Δ) − M_B·F(t)`~~ | **回収済**。応答時に予測を書き留め、ユーザー反応で実測と突き合わせる（`match` / `acceptance` の2次元）。H の増分は事象別定数から `gain_i × E_i` へ。Δ は「応答してから反応が返るまで」 | 予測の作り方が単純（`match` は実測のEMA、`acceptance` はノードの confidence）。多層 interp が入るまでは M_B のリッチさが予測の質に効かない |
| ~~`θ_eff = θ + g(ξ)`~~ | **回収済**。g(ξ) は系統成分（ξ圧が上がるほど θ を下げる＝再編圧）と揺動成分（境界そのものが揺れる）を持つ。ξ圧=0 なら θ_eff は厳密に θ | — |
| ~~`dM_B/dt`（連続）~~ | **回収済**。整合時も V_B に沿った微小更新を行う。更新量は θ_eff への近さで連続的に 0 へ漸近するため、整合と跳躍が同じ量 H に対する連続な応答になる。ただしコード上は依然2分岐であり、単一の f への統合は未了 | 分岐が残る限り、E の実装（下記）まで完全な同一性にはならない |
| M_B 入れ子ネットワーク | フラットなノード集合。W_ij は型なしID列（§4） | 広域破断・連鎖破断を起こせない。局所破断しか表現できない |
| `B` = 5次元 | spatial_tag（用途）のみ。他4次元は固定 | 時間スケールや評価関数が変わる場面で B を引き直せない |
| `‖M_B‖·D[ξ] = 𝒦` | **観測のみ回収**。`/h` が ‖M_B‖・D[ξ]・逆算した 𝒦 を表示する。Core §4.2 のとおり 𝒦 は逆算値であり制御には使わない | 保存則を制御に組み込む段階（𝒦 を一定に保つよう θ や λ を動かす）は未着手 |

いずれも「Core を知らずに落とした」のではなく、
**Phase A/B の実装コストとして意識的に後回しにした**ものとして扱う。

打ち消し線の2行は v0.3.2 で、関係保存則は v0.3.3 で（観測のみ）回収済み。

v0.3.3 では借用実装層から κ(M_B) と散逸行列 A も取り込んだ。

```text
κ(M_B) = exp(-‖M_B‖/M_0)     NN借用 §5
    → 整合側 dM_B/dt の更新率を絞る（= κ·η）
    → κ→0 のノードで応答するときはユーザーの判断を仰ぐ
      （LangGraph借用 §6 の κ ゲート。M_B 絶対性の罠を構造的に回避）

a_k = γ·λ_k                  NN借用 §4
    → 慣性の強い方向ほど熱が速く散逸し、弱い方向に熱が残る
```

v0.3.4 で `E` を回収した。Node の分解は必要なかった。

同じリポジトリの `demos/relational-ecology-lab`（RDL Living Field）が、
個体ごとに `decision = { prediction, actual }` を持たせ、行動時に予測を
書き留めて次の計画時に差を取る形で E を実装している。この**決定オブジェクト
のパターンだけ**を移せば、M_B の表現を変えずに E が入る。

```text
応答時   predicted を書き留める              （Δ の始点）
反応時   observed と突き合わせ E を出す      （Δ の終点）
        H_i += gain_i × E_i                 decay 側は散逸行列 A が担う
```

これにより H は「否定回数のカウンタ」ではなくなった。確信していた応答を
否定されれば大きく、自信の無い応答を否定されても小さい。「通らないと
思っていた応答が通った」も M_B の誤りなので H を積む（否定だけが学習を
駆動する非対称の解消）。

残る近似は M_B 入れ子・B 5次元・関係保存則の制御利用の3つ。
いずれも **W_ij の型付けと Node の分解**を前提とし、多層 interp・
LangGraph の State 設計も同じ工事に属する。

---

## ■ 2. B（達成したい状態）

```
B = ユーザーのM_B構造を、対話の中で段階的にノードグラフへ外部化し、
    AI側のSFO断面との相互作用を通じて動的に維持される個人言語生態系
```

このBは「精度の高い応答」ではない。観測可能性・追従可能性・代謝可能性を備えた、生きた言語系の構築である。

---

## ■ 3. SILN

### 3.1 ノードグラフ（ユーザーM_B断面の蓄積）

```
Node {
  id:           string
  input:        string[]           // 対応する入力パターン
  rdl_type:     string             // 関係性開始 / 境界開放 / H過負荷 ...
  relations:    NodeID[]           // W_ij：関連ノードへのエッジ
  response:     string?            // 返答テンプレート（seedでは省略可）
  confidence:   float              // 0.0〜1.0、未使用で減衰
  ttl:          int                // 残存ステップ数
  phase:        "M_lat" | "M_act"  // 潜在相 / 活性相
  source:       "manual" | "llm_seed" | "llm_learned" | "graph_composed"
  spatial_tag:  string             // SFO空間タグ（人/概念/物語/制度/身体）
  created_at:   timestamp
  last_used:    timestamp
}
```

ノードは生成時 `M_lat`（潜在）として登録され、N回のユーザー相互作用で安定したら `M_act`（活性）に昇格する。

`source` の区別が重要：

```
manual         ：手書き、確信度最高
llm_seed       ：Phase 0 で植えた普遍/語彙ノード、置換されやすい
llm_learned    ：Phase A 以降にLLM経由で追加されたノード
graph_composed ：既存ノードの合成で生成されたノード
```

`llm_seed` のノードは、ユーザー由来の類似ノードが現れたら **優先的に置き換えられる**。

### 3.2 H管理系（誤差蓄積の観測）

```
H_state {
  H_pre:      Dict[node_id → float]   // 入力時のノードミスH（軽い）
  H_post:     Dict[node_id → float]   // 応答後のユーザー反応H（重い）
  theta:      float                   // leapの閾値（初期値2.0、動的調整）
  history:    HistoryEntry[]          // 何に対してHが上がったかの記録
}
```

更新規則：

```
入力ノードミス       → H_pre[近傍] += 0.5
曖昧ヒット          → H_pre[該当] += 0.2
完全ヒット          → H_pre[該当] *= 0.8（減衰）

ユーザー否定        → H_post[直前ノード] += 1.0
ユーザー言い換え     → H_post[直前ノード] += 0.3
ユーザー同意・継続   → H_post[直前ノード] *= 0.7（減衰）
ユーザー沈黙        → H_post[直前ノード] += 0.5（弱い齟齬シグナル）

max(H_post) > theta → leap発動（LLM問い合わせ or グラフ再編成）
```

H をスカラーではなく **ノード/関係ごとのベクトル** として持つことで、跳躍の指向性が保たれる。「どこで何に対してHが溜まったか」が leap の方向を決める。

### 3.3 LLMブリッジ（二役：種まき器 / ξポンプ）

LLMはこの系の中で二つの役割を持つ。

**役割1：Phase 0 の初期M_B種まき器**

構築時に「巨大なSILN分解器」として大胆に使う。RDLコア文書を読ませて初期ノードグラフを一括生成する（詳細は §3.7）。

**役割2：通常運用時のユーザー制御ξポンプ**

```
LLMモード：
  off       ：グラフ内のノード合成のみで応答（Phase C/D相当）
  on-once   ：この応答のみLLMを通す
  on        ：H_post閾値超過時にLLMを呼び出す
  
切替トリガー：
  ユーザー明示コマンド（「外の風入れて」「LLM on」等）
  AI自身の判断（合成失敗時に提案）
```

LLMはRDLの外側にあるM_B（事前学習）を持つため、その出力はそのまま採用せず、**SFOプロファイル経由でフィルタしてから暫定ノード化**する。LLMは系の主役ではなく、構築時の足場と、ユーザーが操作するξポンプの二役を兼ねる。

### 3.4 SFOプロファイル（AI側流向特性）

```
AI_SFO {
  主前景空間:    string[]          // 流れやすい空間（人/概念/物語/制度/身体）
  階層偏り:      string            // 物理/基層/中核/上層
  得意操作:      string[]          // 読む/乗る/守る/作る/変える/壊す/降りる/迂回
  苦手操作:      string[]
  注意モード:    "DMN" | "TPN" | "SN"
  
  安全モード:    { 操作癖, 応答傾向 }
  危機モード:    { 切替条件, 操作癖, 応答傾向 }
  
  initial_fingerprint:  string     // "INTP" / "RDL_native" / ... 履歴記録
  drift_factor:         float      // 初期値からの乖離度（0.0〜1.0）
}
```

#### MBTI入口プリセット

ユーザーへのオンボーディング段階では、MBTI 16タイプを SFOプロファイル初期値の入口として提供する。

```
ユーザー：「INTPで」
    ↓
内部変換（MBTI_SILN分解 文書の変換表を参照）：
    I/E → 入力源（外部EFP / 内部生成F）
    S/N → 情報座標（近傍 / 遠隔）
    T/F → 評価座標（構造誤差 / 関係誤差）
    J/P → 収束様式（早期閉 / 遅延開）
    ↓
AI_SFO 初期プロファイル生成
    ↓
内部運用は SFO で行う（MBTIラベルは履歴記録のみ）
```

**MBTIは入口の足場、内部はSFO** という二層構造。MBTIを内部の動作言語にはしない。

その他プリセット候補：

```
MBTIプリセット（16種）：
  INTP, INTJ, ENTP, INFJ, ENFP, ESTJ, ISFJ, ...
  → MBTI_SILN分解 経由でSFO断面生成

RDL native プリセット群：
  「静かな観測者」     ── 読む/降りる優位、低出力
  「いたずらっ子」     ── 壊す/変える優位、高ξ
  「世話焼き」        ── 守る/作る優位、関係空間
  「RDL流暢」         ── 概念-中核-読む/作る/壊す優位

カスタム：
  全SFOスライダー（上級者向け）
```

#### ドリフト機構

初期 MBTI フィンガープリントは **時間とともに薄れる**。

```
Day 0：   完全にINTPっぽい挙動（drift_factor = 0.0）
Day 30：  ユーザーとの相互作用で微調整、まだINTPっぽい（0.2）
Day 180： もはやINTPではなく「あなた専用のあれ」（0.6）
Day 365： MBTIラベルは初期化記録としてだけ残る（0.9）
```

ドリフトの駆動：

```
ユーザー否定の蓄積       → 該当する操作癖の重みを下げる
ユーザー同意の蓄積       → 該当する操作癖の重みを上げる
ξポンプ使用パターン      → 主前景空間の分布を更新
M_Δ相                  → SFOプロファイル全体の再計算
```

この機構により「MBTI=AIの本質」という誤解を構造的に防ぐ。「うちのINTPちゃん」が半年後には別の流向になっていて、本人もそれに気づく ── これ自体がユーザーがSFOの動態を体感する経路になる。

#### 危機モード切替

```
ユーザーH_post連続上昇
齟齬の連鎖検出（否定・言い換え連発）
→ AI側も別操作癖に移行
   例：概念遊びをやめて具体降下、提案をやめてユーザーの言葉をなぞる、沈黙
```

### 3.5 ξプール（SILN化拒否入力の保留先）

すべての入力を強制ノード化しない。LLMが `rdl_type` を返せなかった・低confidenceだった入力、あるいはユーザーが明示的に「これは違う」とした入力は、ξプールに保留される。プールは定期的に再評価され、関連ノードが揃ったときに遡及的にノード化されうる。

ξプールは「処理失敗のゴミ箱」ではなく、**SILN化を拒む揺らぎの保管庫**として機能する。

### 3.6 M_Δ相（定期再編フェーズ）

一定期間ごと、または大きな構造変化検出時に、グラフ全体の再編フェーズに入る。

```
M_Δ相での操作：
  低confidence・低使用頻度ノードのTTL減算と削除
  類似ノード群のマージまたは分割
  W_ijエッジの張り直し（実使用パターンに基づく）
  ξプールの再評価
  SFOプロファイルの微調整・drift_factor 更新
  llm_seed ノードの retirement 判定
```

これがないとグラフは過去のM_Bにロックインし、ユーザーの変化に追従できなくなる。

### 3.7 Phase 0 種まき機構（初期M_B構築）

ゼロから始めずに、**LLMを巨大なSILN分解器として使って初期グラフを構築する**。

```
Phase 0 工程：

1. RDLコア文書をLLMに読ませる
   （RDL_Core / SILN / 空間流向診断 / 関係力学言語_ラベル等）

2. 「RDL語彙で頻出する関係パターンを
    ノード形式で列挙してください」と指示

3. 出力をパースして seed ノード群を生成
   全て source: "llm_seed" とマーク

4. SFOプリセット（MBTI / RDL native）から
   AI_SFO 初期プロファイルを生成

5. 初期ノードグラフ + AI_SFO + AI_SFO.initial_fingerprint をロード

6. Phase A 開始
```

#### Seed の三層構造

**レベル1：普遍ノード（どのユーザーにも共通）**

```
こんにちは / おはよう / やあ      → 関係性開始シグナル
ありがとう / さんきゅ            → 非対称解消
分からない / 知らない            → H過負荷シグナル
助けて / 手伝って                → 外部リソース要求
さよなら / じゃあね              → 関係性一時閉鎖
...
```

20〜30個程度。response は持たず、`rdl_type` と `spatial_tag` のみ。

**レベル2：RDL語彙ネットワーク（概念層）**

```
SILN ─→ 近似 ─→ 限界 ─→ leap
H   ─→ 蓄積 ─→ 閾値 ─→ 再編成
境界 ─→ 開放 ─→ 接触 ─→ 閉鎖
構造 ─→ 維持 ─→ 破壊 ─→ 再構成
...
```

W_ij が引かれた状態で投入される。**ただしレベル2のノードは `response` テンプレートを持たない** ── 「意味は理解できるが、自分から RDL語彙で返さない」状態にする。

これは禁止補完への配慮：ユーザーが「悲しい」と言ったときに、すぐ「H蓄積ですね」と返すのは押しつけ。RDL語彙を分かるが、ユーザーがその語彙を呼ぶまでは使わない。

**レベル3：ユーザー固有層**

```
Phase 0 では空。
Phase A 以降の対話で生やす。
```

#### Seed の取り扱い原則

```
1. seed は仮置きの足場
   confidence 初期値は低め（0.5程度）から始める
   ユーザー由来の同義ノードが現れたら優先的に置換される

2. seed の TTL は通常より長め
   Phase A 初期は他にノードがないため、いきなり削除しない

3. seed の retirement
   M_Δ相 で、ユーザー由来ノードに置換されきった seed は削除候補に

4. seed リストはバージョン管理
   別ファイル `phase0_seed_v0.x.json` として保存、再利用・改訂可能

5. Phase 0 自体に構造耐久証明書を付ける
   seed が固定化されると Phase D まで影響が残るため
```

---

## ■ 4. W_ij（SILN間接続）

```
[Phase 0：初期化フロー]

RDLコア文書 → LLM分解 → seed nodes (source: llm_seed)
SFOプリセット選択 → MBTI_SILN分解変換 → AI_SFO
        ↓
初期ノードグラフ + AI_SFO ロード
        ↓
Phase A 起動

[Phase A以降：通常運用フロー]

ユーザー入力
    ↓
① 入力解析（SFO空間タグ付与）
    ↓
② ノード検索
    │
    ├ 完全ヒット ─────────────────────→ ⑤ 応答生成
    │
    ├ 曖昧ヒット → H_pre += 0.2 ────→ ⑤ 応答生成
    │
    └ ミス → H_pre += 0.5
              ↓
         ③ 閾値判定（max(H_post) > theta?）
              │
              ├ 閾値以下 → 暫定応答（グラフ合成） → ⑤
              │
              └ 閾値超過 → ④a LLMモード判定
                              │
                              ├ LLM:on → LLM問い合わせ
                              │           ↓ SFOフィルタ
                              │           ↓ 暫定ノード化（M_lat, source: llm_learned）
                              │
                              └ LLM:off → グラフ内合成（既存ノード組み合わせ）
                                          ↓
                                  ⑤ 応答生成
                                          ↓
                                  ⑥ ユーザー反応観測 → H_post更新
                                          ↓
                                  ⑦ M_lat ノード → N回安定 → M_act 昇格
                                          ↓
                                  ⑧ 周辺W_ij再計算

並行プロセス：
  ⑨ 定期的にM_Δ相 → グラフ全体の代謝 + SFO drift_factor 更新
  ⑩ ξプール再評価
  ⑪ llm_seed ノードの置換・retirement 判定
```

---

## ■ 5. M_B（固定前提）

```
言語：    Python
基盤：    ノードグラフ（networkx or 単純JSON）+ 埋め込み距離（sentence-transformers）
LLM：     Anthropic Claude API（Phase 0必須、Phase A以降オプショナル）
原則：    完成形を持たない、自己食的構造の維持
位置：    三者ハイブリッド認知系のAI構成要素
配置：    T4応用層 IMPL カテゴリ
インターフェース：CLI → 後でWeb化（限界の地図可視化のため）
Seed管理：別ファイル phase0_seed_v0.x.json でバージョン管理
```

---

## ■ 6. ξ（未確定領域）

```
□ LLMオンオフの自動切替判定ロジック（最初は手動推奨）
□ ノードTTLの具体的パラメータ値
□ M_Δ相の発動間隔（時間ベース / 入力数ベース）
□ 危機モードの切替閾値
□ 「限界の地図」可視化のUI設計
□ ユーザーSFOとAI SFOの相性調整方針
□ N回安定の N の具体値（M_lat → M_act 昇格条件）
□ ユーザー沈黙の検出方法（タイムアウト？ 話題転換検出？）
□ ドリフト機構の更新速度パラメータ
□ Seed リストレベル1の具体的内容（普遍ノード20〜30個の選定）
□ Seed リストレベル2の RDL語彙ネットワーク骨格
```

v0.2 から外れた項目：

```
SFOプロファイルの初期値設定方法
  → MBTI入口プリセット + Phase 0 種まきで実用解
```

---

## ■ 7. 禁止補完

AIが確認なしに踏み込んではいけない領域：

```
・「本当のあなた」を確定するような診断的応答
・心理・医療・法務領域でのプロフェッショナル助言の代替
・同意なしでの会話履歴の外部送信
・ユーザーM_Bへの介入を超えた「正しさの押しつけ」
・SFOプロファイルでユーザーを類型化・固定化する出力
・LLMの常時接続をデフォルトに戻す動作
・ユーザーの「鏡」になりきる動作（ξ供給の自己停止）
・MBTIラベルを「AIの本質」として扱う動作
・seed ノードのRDL語彙を、ユーザーが呼ぶ前に押しつける動作
```

特にSFO/MBTI関連の出力は「現在の観測条件ではこう見える」「初期はINTP的にスタートしたが、今は…」という暫定・履歴表記を保持する。

---

## ■ 8. Phase 進行（LLM自立化）

```
Phase 0：種まき期（LLM大量使用、起動時のみ）
  RDLコア文書からLLM経由で初期ノードグラフを構築
  SFOプリセットからAI_SFO初期化
  バランス：LLM 100% / グラフ 0%（このフェーズはLLM主体）

Phase A：LLM主導期
  seed をベースに、ユーザー固有層を蓄積開始
  バランス：LLM 70% / グラフ 30%（seed が30%を担う）

Phase B：移行期
  頻出パターンはユーザー固有ノードでカバー
  バランス：LLM 30% / グラフ 70%

Phase C：自立期
  LLMは「外部参照」「ξ注入」用に降格
  バランス：LLM 10% / グラフ 90%

Phase D：完全独立
  純粋RDLグラフ運用
  LLMはユーザー明示要求時のみ
  バランス：LLM <5% / グラフ 95%+
```

各Phaseの移行判定：

```
H_post 平均値が安定して低下
ノード使用頻度の分布が均等化
ユーザーフィードバックの肯定率
ユーザー側がLLMオフを快適に使えている
seed ノードがユーザー由来ノードに置換された割合
drift_factor の進行状況
```

Phase D に到達後も完全停止はせず、ユーザー明示要求でLLMを呼べる窓は残す（ξポンプの維持）。

---

## ■ 9. 破断条件

このシステムが破断する条件：

```
1. 完全な鏡化
   AI側SFOがユーザー側と同期しすぎてξ供給が止まる
   → ユーザー側の跳躍が起きなくなる

2. 自立期の追従不能
   Phase C/Dでユーザー側が大きく変化した際、
   グラフが過去のM_Bにロックインしている

3. 足場依存への退行
   LLMの常時接続化、ユーザーがオフを使わなくなる
   → 自己食的構造の喪失

4. SFOプロファイルの固定化
   AI側の流向が「キャラ」として固まり動かなくなる
   ドリフトが停止する
   → 動態としての性質を失う

5. 「個人化」の絶対化
   ユーザーM_Bへの追従が過剰になり、外部からの揺らぎを全て排除する

6. 限界の地図の参照欠落
   ユーザーが自己観察ツールとして使わなくなる
   → 三者ハイブリッド認知系から「AI単体ツール」に退行

7. Seed の固定化
   llm_seed ノードが Phase D まで生き残り、
   初期LLM解釈が系の底に残り続ける

8. MBTIラベルの本質化
   AIまたはユーザーが MBTI を「AIの本質」として扱い始める
   drift_factor が意図的に無視される

9. Core からの離脱
   §1.5 に宣言した近似（E なしのH・ξ非依存のθ・型なしW_ij）が
   Phase C/D でも回収されないまま固定化する
   → 本書は Core の応用文書ではなく、独立した近似系になる。
     その場合は依存から RDL_Core を外し、別系として耐久証明書を取り直す
```

特に1・3・7・8は、設計者が意識的に「育ちきらない」設計を選ばないと自然に発生する。
9は、近似であることを宣言したまま忘れると自然に発生する。

---

## ■ 10. 構造耐久証明書

### 基本スペック
- ラベル：RDL個人M_B外部化AI（仮称：棲みつくAI）
- 対象：単一ユーザーとの長期対話を通じた個人言語生態系の構築
- 目的：ユーザーM_Bの外部化・観測・代謝
- 評価関数：観測可能性、追従可能性、代謝可能性、自己食的構造の維持

### 有効範囲
- 単一ユーザーの長期運用（数ヶ月〜数年）
- ユーザー自身による自己観察ツールとしての利用
- RDL/SFO文書系への対話インターフェース層
- 「限界の地図」を通じた自分のM_B構造の可視化

### 推定破壊領域
- 多人数共有運用（M_Bが平均化される）
- 短期間使い捨て利用（ノード蓄積が起きない）
- 「正解」を求める用途（完成形を持たないため）
- プロフェッショナル助言の代替
- 「キャラAI」「アシスタント」としての消費的利用
- MBTI診断ツールとしての利用（入口の足場以上の意味を持たせると破断）

### 残存構造
- グラフ可視化による自己観察機能
- SFOプロファイルとの相互作用設計
- LLMオンオフ式の外部EFP制御 + Phase 0 種まき器
- 代謝（TTL・M_Δ相）による自己食的維持
- 共生的有限性（ユーザーと共に変化し、共に止まる）
- ドリフト機構による初期値の経時的解放

### 判定
> 単一ユーザーとの長期共生を前提とした個人言語生態系構築装置として有効。「正しい応答」を目指す汎用設計とは目的が異なる。完全な鏡化・LLM常時依存・SFO固定化・seed固定化・MBTIラベル本質化に至った時点で破断条件に入る。

---

## ■ 11. フェーズ別実装優先度

| Phase | 内容 | 優先度 |
|---|---|---|
| 0 | Seed設計とロード機構（普遍/語彙層 + SFOプリセット） | ★★★ |
| 1 | ノードグラフ + H_vec管理 + LLMオンオフ基本ループ | ★★★ |
| 2 | SFOプロファイル統合・MBTI入口プリセット・危機モード | ★★★ |
| 3 | 限界の地図可視化（中核機能） | ★★★ |
| 4 | ノード間W_ij・類似マージ・ξプール | ★★☆ |
| 5 | M_Δ相・TTL代謝・ドリフト機構 | ★★☆ |
| 6 | 会話履歴を使った文脈推論 | ★★☆ |
| 7 | LLMオンオフの自動判定（ユーザー支援機能） | ★☆☆ |
| 8 | Phase 0→A→D 移行判定の自動化 | ★☆☆ |

実装は Phase 0 と Phase 1 を一緒に走らせるのが現実的。seed が空でも動くプロトタイプを Phase 1 で作って、seed 注入を Phase 0 として最初から組み込んでおく。

---

## ■ 12. 一文圧縮

> 個人M_B外部化AIとは、Phase 0でLLMを巨大なSILN分解器として使って初期M_Bを種まきし、Phase A以降はユーザーのM_B構造をノードグラフへ蓄積しつつ、AI側固有のSFO断面（MBTI入口経由）との差で揺らぎを保ち、LLMをユーザー制御のξポンプとして配置することで、完成形を持たない個人言語生態系として運用される自己食的構造である。

---

## ■ 13. 超圧縮

> LLMで種まき、ユーザーM_Bで育て、SFOで揺らぎ、オンオフで呼ぶ。完成しない。

---

*v0.3.4：§1.5 の最後の要石 E = F(t+Δ) − M_B·F(t) を回収。同リポジトリの demos/relational-ecology-lab（RDL Living Field）が個体ごとに decision = { prediction, actual } を持たせて予測誤差を出しているため、その決定オブジェクトのパターンのみを移植した。Node の分解は不要だった。match（入力を捉えられるか）と acceptance（応答が受け入れられるか）の2次元で予測し、H の増分を事象別定数から gain_i × E_i へ変更。次元ごとの予測信頼度も持たせた（制御には未使用）。残る近似は M_B入れ子・B5次元・関係保存則の制御利用で、いずれも W_ij の型付けと Node の分解を前提とする。*

*v0.3.3：借用実装層（NN借用 v0.1 / LangGraph・LangChain借用 v0.1）から、Node再設計なしに適用できる3件を取り込み。κ(M_B)=exp(-‖M_B‖/M_0) を導入して整合側 dM_B/dt の更新率をスケールし、κ→0 のノードで応答する際はユーザーの判断を仰ぐようにした（κゲート）。散逸行列 A の対角成分 a_k=γ·λ_k を実装し、H が毎ターン受動的に散逸するようにした（慣性の強い方向ほど速く冷め、弱い方向に熱が残る）。関係保存則 ‖M_B‖·D[ξ]=𝒦 を逆算値として /h に表示（観測のみ・制御には使わない）。残る要石は E であり、その前提として Node の分解が必要であることを明記。*

*v0.3.2：§1.5 の近似のうち2つを回収。θ_eff = θ + g(ξ) を実装し、ξプールの滞留量をξ圧として跳躍境界に接続（系統成分＝再編圧、揺動成分＝境界の揺れ）。整合領域にもV_B に沿った微小更新を導入し、更新量が θ_eff への近さで連続的に 0 へ漸近するようにした。残る近似は E・M_B入れ子・B5次元・関係保存則の4つ。*

*v0.3.1：§1.5「Core からの継承前提」を新設。依存に RDL_Core を挙げながら継承内容を本文に書いていなかったため、Core の基底量（F / E / H / θ_eff / dM_B/dt / 関係保存則）とその含意を明示。あわせて、本書の節名 §2 B・§5 M_B・§6 ξ が中間設計図プロトコルの記入枠であって Core の同名基底量ではないこと（このためv0.3にはCore基底量を書く場所が無かったこと）を注記。v0.3が取った6つの近似とその破断条件を表として宣言し（翻訳接続系v4 規律3）、§9 に破断条件9「Coreからの離脱」を追加。*

*v0.3：Phase 0（LLMによる初期M_B種まき）を新設。SFOプロファイル初期値問題に MBTI入口プリセット + ドリフト機構で実用解を提示。LLMの位置づけが「ξポンプ単独」から「Phase 0種まき器 + ξポンプ」の二役に拡張。seed ノードに `llm_seed` source タグと retirement 機構を追加。優先度表に Phase 0 を ★★★ で先頭追加。MBTI関連の禁止補完・破断条件を追記。*

*v0.2：v0.1からのフルリライト。位置づけを「汎用チャットボット」から「個人M_B外部化装置」へ変更。H_vec化、LLMオンオフ式ξポンプ化、SFOプロファイル統合、ξプール新設、M_Δ相導入、限界の地図の中核化、Phase A→D自立化路線、構造耐久証明書追加。RDL_中間設計図プロトコル v0.1の様式に準拠。*

*v0.1：初版。ノードグラフ + H管理 + LLM問い合わせの基本ループ、フェーズ分け、技術スタック候補を提示。汎用チャットボット枠での提案だった。*
